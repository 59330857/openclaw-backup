# 2026-06-05

## tts 状态排查

### 用户原话
- "tts 原来一直使用2.7也没有成功过，昨天才刚换上的"

### 数据事实（已核对）
- **tts 是个独立 OpenClaw agent**，配置目录 `/home/halei/.openclaw/agents/tts/`，workspace 在 `/home/halei/workspace-tts/`
- tts 5月25日以来所有 cron session 的 `model_change` 事件都是 `minimax/MiniMax-M2.7`
- tts 的 `agent/models.json` 只有一个 provider：minimax，只有一个模型 M2.7（5月8日建，未更新）
- tts 的 `agent/config.json` 写的是 `model: "gemini/gemini-2.0-flash"`（5月2日建，过时），但实际跑的是 M2.7
- tts 的 `agent/auth-profiles.json` 只有 gemini 的 apiKey（5月2日建）

### "昨天才刚换上的" 数据对不上
- 6月4日 23:10 最新的 model_change 事件 **仍是 M2.7**
- 6月4日没有切换到 M3 或其他模型的痕迹
- 几种可能：① 改了 config.json 但没让 tts 重启；② 记错了时间，实际一直就是 M2.7；③ 换了别的什么（sorftime key、mcporter 等）但不是模型
- **待向用户确认"换上"具体指什么**

### tts 6月4日 cron 实际跑的结果
- 22:54 触发 TK选品-越泰黑马拦截-v6.0
- 之前 6 次都是 "AI service overloaded" 错误
- 这次实际执行发现：
  - 越南站能拿到数据，30个候选商品
  - **泰国站 tiktok_similar_product API 报错**（多次）
  - **Sorftime 越南站缺 `新增带货视频数` 字段**——SOP 要求的"带货视频≥8个"无法直接获取，改用替代条件（价格$1.5-$8 + 星级≥4.2）筛出 25 个发饰候选
  - 最后一条消息：`[assistant turn failed before producing content]` — M2.7 推理过程中挂掉
- 这次任务没成功写飞书多维表

### 历史背景
- 5月19日：M2.7 跑通过一次，写入 10 条飞书多维表，但毛利率 8-14% 远低于 SOP 30% 标准
- 5月20日：sorftime MCP key 更新
- 5月24日：修复 BUG（MCP key shell 变量截断、bitable app_token 截断、MultiSelect 字段格式）
- 5月27日：Sorftime API 配额耗尽（"went dark"）
- 6月3日：购买 30000 次 Sorftime 额度
- 6月4日：cron 仍持续触发，但实际产出有限
